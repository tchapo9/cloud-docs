// @ts-check
const {themes: prismThemes} = require('prism-react-renderer');

/** @type {import('@docusaurus/types').Config} */
const config = {
  title: 'Rapport Cloud Computing — EC2LT',
  tagline: 'Virtualisation, Proxmox HA & Cloud privé OpenStack',
  favicon: 'img/favicon.ico',

  url: 'https://tchapo9.github.io',
  baseUrl: '/cloud-docs/',

  organizationName: 'tchapo9',
  projectName: 'cloud-docs',

  onBrokenLinks: 'warn',
  onBrokenMarkdownLinks: 'warn',

  i18n: {
    defaultLocale: 'fr',
    locales: ['fr'],
  },

  presets: [
    [
      'classic',
      /** @type {import('@docusaurus/preset-classic').Options} */
      ({
        docs: {
          routeBasePath: '/',
          sidebarPath: require.resolve('./sidebars.js'),
          editUrl: undefined,
        },
        blog: false,
        theme: {
          customCss: require.resolve('./src/css/custom.css'),
        },
      }),
    ],
  ],

  themeConfig:
    /** @type {import('@docusaurus/preset-classic').ThemeConfig} */
    ({
      image: 'img/docusaurus-social-card.jpg',
      navbar: {
        title: 'Cloud Computing — EC2LT',
        items: [
          {
            type: 'docSidebar',
            sidebarId: 'docsSidebar',
            position: 'left',
            label: 'Documentation',
          },
        ],
      },
      footer: {
        style: 'dark',
        links: [],
        copyright: `Rapport présenté par Tchapo TCHEDRE, Emile Royal MOLOWA ESSONGA, Leonel, Clevy BANZOULOU, Junior — EC2LT · Sous la supervision de Mr. BASSENE`,
      },
      prism: {
        theme: prismThemes.github,
        darkTheme: prismThemes.dracula,
      },
    }),
};

module.exports = config;
